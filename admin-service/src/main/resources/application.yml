server:
  port: 8084

spring:
  application:
    name: admin-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yml
        refresh-enabled: false
  config:
    import:
      - nacos:admin-service.yml
