# DeepSeek API Key（勿提交到 Git）
# K8s: kubectl create secret generic carpool-secrets --from-literal=deepseek-api-key=YOUR_KEY -n carpool --dry-run=client -o yaml | kubectl apply -f -
DEEPSEEK_API_KEY=CHANGE_ME_DEEPSEEK_API_KEY
